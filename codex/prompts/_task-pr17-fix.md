PR #17 스토리 바이블 V1 의 Codex TM 종합 검토 + fix (v3 [4], 리뷰+fix 일체). 먼저 7파트 관점 종합 검토 + Claude TM 리뷰 대조를 stdout 출력(PM 이 게시). 그 다음 아래 fix:

fix (일관성 — Claude TM [3] 지적):
1. **챕터1 맵명 통일**: 현재 docs/planning/06-story-bible.md §5.1 과 docs/planning/01-game-design.md §5.2 의 맵명이 2건 불일치:
   - 1-2: 바이블 "작은 버섯숲" ↔ GDD "늑대의 숲"
   - 1-4: 바이블 "망령의 언덕" ↔ GDD "망령의 폐허"
   → **바이블의 명칭/컨셉을 canonical 로 채택**하고, GDD `01-game-design.md §5.2` 표를 바이블과 동일하게 갱신(맵명·컨셉). 두 문서가 완전히 일치해야 한다(레벨은 이미 일치).
2. **1-5 / 챕터2 명칭 중복 해소**: 챕터1 1-5 맵명이 "잊혀진 신전" 으로 챕터2 명("잊혀진 신전") 과 겹친다. 챕터1 1-5 를 "폐신전 입구" 등으로 바꿔 구분(바이블 §5.1 + 진행요약 + GDD 일관 반영).
3. 검토 중 추가 GDD 모순(고유명사/연표/세력) 발견 시 바이블 또는 해당 문서 정합.

제약: 전부 한글. 스토리 톤 유지. **레벨/구조 변경 금지**(맵명/컨셉 정합만). markdownlint 통과 유지. push 금지. 커밋 prefix: codex(story):.
