## Codex TM 리뷰 + fix 산출 (단계 [4])

커밋 `b911a74` `codex(qa): 직업 스킬 parity 검증 보강`

### Claude TM 권장 처리
- **parity 확장**: `IdleProject.Combat.Skills.DefinitionParity` 가 3직업(전사/마법사/궁수) 전체 스킬 server↔client 동일성 검증.
- **직업 스토리 분기**: 스토리 직업별 인트로 + StoryText.csv 3줄.
- **밸런스 문서**: Mage(고DPS)/Archer(치명·공속) 가드레일 추가.
- QA 시나리오 `docs/qa/scenarios/M5-mage-archer-classes-v1.md`.

### 검증
- 서버 `npm test` **115 passed**, build exit 0, **lint exit 0**.
- UE Build Succeeded, Automation **39 tests** (DefinitionParity 포함) Success, EXIT 0. 신규 QA 문서 markdownlint 0 errors.

참고: 기존 docs 전체 markdownlint baseline 오류(971)는 별도 범위(이번 신규 문서는 통과, CI markdown lint 잡은 통과).

→ 다음: Claude 검증(단계 [5]) — PM 직접 재검증 + CI 그린 확정.
