# 챕터7 콘텐츠 구현 계획

> 스펙: [`2026-05-30-chapter7-design.md`](../specs/2026-05-30-chapter7-design.md). #66/#93 멀티챕터 패턴 재사용. **세이브 무변경**.

**Goal:** 챕터7(스테이지 61~70) 추가 — 약점/스토리/메인 퀘스트. 데이터 구동 저위험(#93 ch6 동형).

## Task 1: character+backend (character)
- [ ] `StageService.h` `TotalChapters` 6→7.
- [ ] 서버 `stages.ts`: 글로벌 스테이지 61~70(ch6[51~60] 연속 스케일 외삽, 7-5/7-10 엘리트·보스 자동). 약점 61~70(5속성 Dark 가중 ch6 패턴, default None 없게) — 클라 `StageFormula`(GetStageWeakElement) ↔ 서버 `stage.ts` 1:1.
- [ ] 클라↔서버 stage parity. 검증 테스트(stages.test.ts + 클라 StageServiceTests): ch7 스테이지 61~70 존재·7-5 엘리트/7-10 보스·약점 61~70 비-None·TotalChapters 7.
- [ ] `cd server; npm run lint && npx vitest run src/core/data && npm run build` GREEN.
- [ ] 커밋 `feat: 챕터7 스테이지/약점 (챕터7)`.

## Task 2: story (story)
- [ ] 스토리 바이블 ch7 + StoryText/Story CSV(ch7 내러티브, ch6 붕괴 후속). ko/en. 키 규약 STORY_C07_*/STORY_CHAPTER_07_*/STORY_MAP_C07_M01~M10_*/STORY_ELITE_C07_*/STORY_BOSS_C07_*(#93 패턴). 스토리 전용 키만.
- [ ] 커밋 `feat: 챕터7 스토리 (챕터7)`.

## Task 3: quest (quest)
- [ ] 서버 `quests.ts` + 클라 `QuestService`: ch7 메인 퀘스트 6개(체인, 클리어 조건 ch7 스테이지/보스 글로벌 61~70). DefinitionParity 47→53(클라↔서버 1:1). Quest.csv ko/en 6키. vitest(quests)+클라 퀘스트 테스트.
- [ ] 커밋 `feat: 챕터7 메인 퀘스트 (챕터7)`.

## Task 4: designer (designer)
- [ ] `Tests/LocalizationTests.cpp` ch7 필수키 게이트(RequiredChapter7StoryKeys/StoryTextKeys/QuestKeys — 실제 커밋 키와 일치, #93 패턴). HUD 자동 표시 확인(코드 0). CsvIntegrity. 표준 jumbo.
- [ ] 커밋 `feat: 챕터7 로컬라이즈 게이트 (챕터7)`.

## Task 5: balance/qa (balance)
- [ ] `docs/planning/chapter7-balance-note.md`(ch7 스케일 ch6 연속·약점 분배·세이브 무변경) + QA E2E(ch7 진행/엘리트/보스/약점/퀘스트 체인/parity). 표준 jumbo + ue-automation.ps1 게이트(**IdleProject.UI.HUD 포함**). [[project_pr_order]]/[[project_session_progress]] 갱신.
- [ ] 커밋 `docs+test: 챕터7 밸런스/QA (챕터7)`.

## Self-Review
- 스펙 §3 전부 매핑 ✓. parity: stages.ts/quests.ts ↔ StageService/QuestService(스테이지/스케일/약점/퀘스트 DefinitionParity) — TM cross-check.
- **세이브 무변경**(멀티챕터 일반화, 글로벌 idx, #93 입증). 약점 61~70 명시(#70). LocalizationTests ch7 게이트(#93).
- 스테이지 스케일 ch6 연속(파워크리프 가드). 게이트 필터에 UI.HUD 포함(#96/#97 교훈).

## 매핑: 1→character(메인+backend), 2→story, 3→quest, 4→designer, 5→balance/qa.
